import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  titleContainer: {
    display: "flex",
    width: "100%",
  },
  title: {
    flex: 1,
    height: 48,
  },
});

export default useStyles;
